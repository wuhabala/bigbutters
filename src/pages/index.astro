---
// src/pages/index.astro
// 临时 smoke 页 · 用于验证 base.css 加载、字体生效、prose 作用域
// Task 17 实现真正的 V1.1 首页时会被替换。
import '@styles/reset.css';
import '@styles/base.css';
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>大大黄油 · WIP</title>

  {/* 只 preload 首屏会用到的字体 Regular weight；600 与 italic 按需 lazy load */}
  <link rel="preload" href="/fonts/NotoSerifSC-Regular.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/fonts/EBGaramond-Regular.woff2" as="font" type="font/woff2" crossorigin />
</head>
<body>
  <main style="max-width: 760px; margin: 0 auto; padding: 48px 32px;">
    <p style="font-family: 'EB Garamond', serif; font-size: 11px; letter-spacing: 0.4em; text-transform: uppercase; color: var(--accent);">
      🪳 bigbutters · phase 1 · work in progress
    </p>

    <article class="prose" style="margin-top: 24px;">
      <h1>大大黄油</h1>
      <p>个人交互式学术策展空间——<strong>外壳搭建中</strong>。</p>

      <blockquote>
        读得慢，写得久——把每一段思考当作一处地形。
      </blockquote>

      <h2>视觉系统验证</h2>
      <p>
        这一段用 <em>italic</em> 验证斜体；用 <strong>strong 加重</strong> 验证 600 字重；
        以及一个 <a href="/about/">链接样式</a> 验证 prose 范围。
      </p>

      <hr />

      <h3>下一阶段</h3>
      <p>
        Tasks 4-7（主题系统 + Content Collections）→
        Tasks 8-12（核心组件）→
        Tasks 13-14（Layouts）→
        Tasks 15-17（路由 + 真正的首页 V1.1）→
        Tasks 18-22（占位内容 + 模板 + 部署 + 验收）。
      </p>
    </article>
  </main>
</body>
</html>
